import { Typography, Box } from '@mui/material'

export default function StudentSessions() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        My Sessions
      </Typography>
      <Typography variant="body1">
        Student session management will be implemented here.
        This will include booking new sessions, viewing session history, and joining video calls.
      </Typography>
    </Box>
  )
}