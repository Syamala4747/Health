import { Typography, Box } from '@mui/material'

export default function ChatInterface() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Chat Interface
      </Typography>
      <Typography variant="body1">
        Real-time chat interface for counsellors will be implemented here.
        This will include message history, file sharing, and crisis detection alerts.
      </Typography>
    </Box>
  )
}